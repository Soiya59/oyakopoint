/**
 * S14 家族の木（「木」タブの入口）
 *
 * [2026-09-09新規追加・実装メモ.md 186章] 統括の実機確認「木はどこ？」を受け、
 * 「かぞく」タブの中のショートカットだった家族の木を、常設の第3タブへ昇格させた。
 * 「新しい画面を作るのではなく、既存の木の画面をタブの位置に持ってくる（または
 * タブから既存画面へ委譲する）」（本部長指示）に従い、実装は変更せず
 * `app/supporter/family-tree.tsx`のコンポーネントをそのまま委譲表示するだけに
 * した。`/supporter/family-tree`というURL自体は、木に飾った直後の遷移先として
 * `app/supporter/tree-decorate.tsx`が引き続き使うため残してある（実装メモ186章）。
 */
export { default } from "../family-tree";
