/**
 * C20 かぞくの木（「木」タブの入口・子ども）
 *
 * [2026-09-10新規追加・実装メモ.md 188章] 子ども下部タブ4区画化に伴い、木を常設の
 * 第4タブへ昇格させた（保護者・みまもりメンバーの実装メモ186・187章と同じ判断）。
 * 「新しい画面を作るのではなく、既存の木の画面をタブの位置に持ってくる」方針のとおり、
 * 実装は変更せず`app/child/family-tree.tsx`のコンポーネントをそのまま委譲表示するだけに
 * した（`app/parent/(tabs)/tree.tsx`・`app/supporter/(tabs)/tree.tsx`と同じ作り方）。
 * `/child/family-tree`というURL自体は、木に飾った直後の遷移先として
 * `app/child/tree-decorate.tsx`・`app/child/gacha-result.tsx`が引き続き使うため残してある。
 *
 * [開発部への申し送り] `app/child/family-tree.tsx`自体には4タブ共通ヘッダー
 * （`ChildTabHeader`）を追加していない。保護者・みまもりメンバーの`family-tree.tsx`も
 * 同様に共通ヘッダーを持たず、末尾に独自の「もどる」ボタン（`router.replace`）が
 * 残ったままになっている（`/parent/family-tree`・`/child/family-tree`は木に飾った
 * 直後の遷移先としても使われており、その文脈では「もどる」導線が必要なため）。
 * 「先行実装をそのまま踏襲し、C20の内部ロジックは変更しない」という設計指示
 * （36.5.4節・36.11節4）を優先し、既存の2ロールと矛盾しない形にそろえた。
 * その結果、木タブだけは4タブ共通ヘッダーを持たない・アバターが押せないという、
 * 保護者・みまもりメンバーと同じ既知の不揃いを子どもにも引き継いでいる。
 * 本部長へ：これを解消する（`family-tree.tsx`にヘッダーを追加する）べきかどうかは
 * 3ロール共通の判断が必要なため、本件では見送った。実装メモ188章に記録した。
 */
export { default } from "../family-tree";
