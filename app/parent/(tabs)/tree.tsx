/**
 * P26 家族の木（「木」タブの入口・保護者）
 *
 * [2026-09-10新規追加・実装メモ.md 187章] 保護者のタブ化に伴い、木を常設の第3タブへ
 * 昇格させた（みまもりの実装メモ186章と同じ判断。統括「木がメインどころでもあるし」）。
 * 「新しい画面を作るのではなく、既存の木の画面をタブの位置に持ってくる」方針のとおり、
 * 実装は変更せず`app/parent/family-tree.tsx`のコンポーネントをそのまま委譲表示するだけに
 * した（`app/supporter/(tabs)/tree.tsx`と同じ作り方）。`/parent/family-tree`というURL
 * 自体は、木に飾った直後の遷移先として`app/parent/tree-decorate.tsx`が引き続き使うため
 * 残してある。
 */
export { default } from "../family-tree";
