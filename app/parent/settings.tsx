import { Redirect } from "expo-router";

/**
 * P17 設定 — 2026-08-29に app/parent/family.tsx（P14）へ統合した。
 *
 * ユーザーの「設定に家族をいれてもよいかも」という提案に対し、本部長から**向きが逆**である
 * と指摘した。設定には「家族を削除する」という不可逆な操作があり、家族管理には招待コード・
 * PIN設定という日常的に開く操作がある。よく使うものを、めったに使わない危険な画面の下に
 * 埋めることになるため、設定を家族へ入れる形にした（ユーザー同意済み）。
 * ホームのメニュータイルは「⚙️ 設定」1つに統合し、「家族」タイルは廃止している。
 *
 * このファイルを削除せずリダイレクトとして残しているのは、`/parent/settings` を指す
 * 導線が他に残っていた場合に404にしないため。
 */
export default function SettingsRedirect() {
  return <Redirect href="/parent/family" />;
}
