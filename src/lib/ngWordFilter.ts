/**
 * NGワードフィルタ（要件定義書07-32章 決定15〜19、設計部/成果物/スキーマ設計.sql
 * 70章、API仕様.md 27章、2026-09-20新設）。
 *
 * [この機能の形]
 * - クライアント側で、送信ボタンを押した時点に判定する。サーバ処理・DBの
 *   トリガー・AI・外部APIは使わない（決定16）。本章はDBに何も作らない
 *   （スキーマ設計.sql 70.5章）。
 * - 語のリストはDBに置かず、アプリのコード内の定数として持つ（決定18）。
 *   更新は軽微変更ルートで行い、企画部・設計部の再承認を毎回要しない。
 * - 判定は正規化（全角/半角・大文字/小文字・ひらがな/カタカナ）したうえでの
 *   部分一致（決定16）。
 * - どの語に当たったかは画面に絶対に出さない（決定16。回避方法を教えて
 *   しまうため）。
 * - 効かせる6欄（決定15）: 掲示板の投稿本文／掲示板のコメント（07-30章・
 *   未実装）／完了報告のひとこと／完了報告へのコメント／感謝メッセージの
 *   ひとこと／お絵かきの題名。絵そのものは対象外（決定19）。
 *
 * [★2026-09-20・本部長へ報告（未確定のまま進めている点）]
 * 決定36により、初期リスト（20〜50語）は企画部が用意することになっている。
 * 本部長の依頼「要件定義書07-32章にあるはずなので、そこから取ること。
 * 見つからなければ手を止めて本部長に報告してください（勝手に語を選ばない
 * こと）」に従い、要件定義書.md（07-32-0f・07-32-5・07-32-12・07-32-13の
 * 4番・10章・11章のいずれも）とスキーマ設計.sql 70章を確認したが、
 * **実際の語のリストはどこにも見つからなかった**（決定36は「企画部が作る」
 * という方針の確定であり、リストの中身そのものはまだ無い）。
 *
 * そのため、開発部としては語を選定せず、NG_WORDS を意図的に空配列のまま
 * にしてある。空配列の間、containsNgWord() は常に false を返す
 * （＝フィルタは機能しない）。企画部から20〜50語のリストが届き次第、
 * このファイルの NG_WORDS 配列だけを更新すればよい（決定18「更新は軽微
 * 変更ルートで行える」。マイグレーション不要、DBには一切触れない）。
 * 参照した根拠（決定36「参照した根拠を1行、社内の記録として残す」）も、
 * リストと同時にこのファイルのコメントへ追記すること。
 */

/**
 * 初期リスト（決定18・決定36）。20〜50語程度。
 *
 * ★未確定・空のまま（上記コメント参照）。企画部からリストが届くまでは
 * 何も判定されない。
 */
export const NG_WORDS: readonly string[] = [];

/**
 * 全角/半角・大文字/小文字・ひらがな/カタカナを正規化する（決定16）。
 * - NFKC正規化で全角英数記号を半角に寄せる（例: "ＡＢＣ" → "ABC"）。
 * - 小文字化する。
 * - ひらがなをカタカナへ寄せる（NG_WORDS側・入力側の双方をこの関数に通すため、
 *   どちらの表記で書かれていても一致する）。
 */
export function normalizeForNgWordCheck(input: string): string {
  return input
    .normalize("NFKC")
    .toLowerCase()
    .replace(/[ぁ-ゖ]/g, (ch) => String.fromCharCode(ch.charCodeAt(0) + 0x60));
}

/**
 * 入力にNGワードが含まれるか（部分一致、決定16）。NG_WORDSが空の間は常にfalse。
 */
export function containsNgWord(input: string, words: readonly string[] = NG_WORDS): boolean {
  if (!input || words.length === 0) return false;
  const normalized = normalizeForNgWordCheck(input);
  return words.some((w) => w && normalized.includes(normalizeForNgWordCheck(w)));
}

/**
 * 止めたときに出す文言（決定17）。企画部案・最終文言はUIUXデザイン部が確定する
 * （開発部/成果物/実装メモ.md参照。画面への組み込みは次回＝UIUX設計後）。
 * 「不適切」「違反」「禁止」「ブロック」「エラー」「警告」「送信できません」は
 * 使わない（決定17の禁止語）。入力した内容を消さない運用にすること
 * （呼び出し側の責務）。
 */
export const NG_WORD_BLOCK_MESSAGE = {
  child: "この ことばは、かぞくが よんだら かなしい きもちに なるかも。べつの いいかたに かえて、もういちど おしてね。",
  adult: "この表現は、利用規約で控えていただいているものです。書き直してからお送りください。",
} as const;
