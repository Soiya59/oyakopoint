/**
 * NFCタグの人ごと化（要件定義書07-2章「作り直し：タグの人ごと化」）まわりの
 * クライアント側共通定数。
 *
 * サーバー側の唯一の定義箇所は `public.max_nfc_tags_per_chore_member()`
 * （設計部/成果物/スキーマ設計.sql 39.3章、常に5を返すSQL関数）であり、
 * 最終的な上限強制はDBトリガー（`chore_nfc_tags_before_write`）が行う。
 * このクライアント側の値はUI側の事前表示（「◯/5まい」表示・選択不可の
 * グレー表示）にのみ使う事前チェックであり、正本ではない
 * （API仕様.md 9章「最終判定は必ずDBトリガーで行う」と同じ設計思想）。
 * サーバー側の値（企画部初期案「5枚」、運用データを見て見直す前提）を
 * 変更する場合は、この値もあわせて変更すること。
 */
export const MAX_NFC_TAGS_PER_CHORE_MEMBER = 5;
